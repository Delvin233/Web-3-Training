// SPDX-License-Identifier: MIT
pragma solidity >0.7.0 <= 0.9.0;

// Data types and varibles
/* variables are places in code that store some information 
    variables retain space in memory as they store a certain value
*/
// Variables used are Booleans, integers and strings

// Booleans : bool: check if something is true or false
// Integers: uint: for integers of many sizes
// Strings: string: ordered sequence of characters

// Since solidity is a contract language, we use the contract keyword

contract LearnVariables{
    uint choco = 10;
    uint owner = 100;
    bool everyCustomer = true; 
    string AreTheyAvailable = " That i cannot tell";

}